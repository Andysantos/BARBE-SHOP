function logar() {
    //validando nome de login
    var nomel = document.getElementById("nomel").value;

    if (nomel.trim() == "") {
                alert("O nome é obrigatório!")
            }
    //validando senha de login
    var senhal = document.getElementById("senhal").value;
    if (senhal.trim() == "") {
                alert("A Senha é obrigatória!");
            }
    }
  function cadastrar() {
      //validando nome de cadastro
      var nomec = document.getElementById("nomec").value;
      if (nome.trim() == "") {
                alert("O nome é obrigatório!")
            }
      //validando sobrenome
      var sobrenome = document.getElementById("sobrenome").value;
      if (nome.trim() == "") {
                alert("O nome é obrigatório!")
            }
      //validando e-mail
      var email = document.getElementById("email").value;
      if (nome.trim() == "") {
                alert("O nome é obrigatório!")
            }
      //validando senha
      var senhac = document.getElementById("senhac").value;



      //validando confirmaçao de senha
      var confsenha = document.getElementById("confsenha").value;
  }